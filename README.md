# Project Documentation

Generated by **SAGE Coding Agent (v6)** — *Build Big. Think Deep. Ship Fast.*

## 🚀 Overview
Generate developer documentation & setup guides for: Review active file

## 🎯 Active Task
Generate developer documentation & setup guides for: Review active file


## 📄 Target File(s)
### Target File: `README.md`
```
# Project Documentation

Generated by **SAGE Coding Agent (v6)** — *Build Big. Think Deep. Ship Fast.*

## 🚀 Overview
Generate developer documentation & setup guides for: Review active file

## 🎯 Active Task
Generate developer documentation & setup guides for: Review active file


## 📄 Target File(s)
### Target File: `README.md`
```
# Project Documentation

Generated by **SAGE Coding Agent (v6)** — *Build Big. Think Deep. Ship Fast.*

## 🚀 Overview
Generate developer documentation & setup guides for: Review active file

## 🎯 Active Task
Generate developer documentation & setup guides for: Review active file


## 📄 Target File(s)
### Target File: `README.md`
```
# Project Documentation

Generated by **SAGE Coding Agent (v6)** — *Build Big. Think Deep. Ship Fast.*

## 🚀 Overview
Generate developer documentation & setup guides for: Review active file

## 🎯 Active Task
Generate developer documentation & setup guides for: Review active file


## 📄 Target File(s)
### Target File: `README.md`
```
# Project Documentation

Generated by **SAGE Coding Agent (v6)** — *Build Big. Think Deep. Ship Fast.*

## 🚀 Overview
Generate developer documentation & setup guides for: Review active file

## 🎯 Active Task
Generate developer documentation & setup guides for: Review active file


## 📄 Target File(s)
### Target File: `README.md`
```
# Project Documentation

Generated by **SAGE Coding Agent (v6)** — *Build Big. Think Deep. Ship Fast.*

## 🚀 Overview
Generate developer documentation & setup guides for: Review active file

## 🎯 Active Task
Generate developer documentation & setup guides for: Review active file


## 📄 Target File(s)
### Target File: `README.md`
```
# Project Documentation

Generated by **SAGE Coding Agent (v6)** — *Build Big. Think Deep. Ship Fast.*

## 🚀 Overview
Generate developer documentation & setup guides for: make a website

## 🎯 Active Task
Generate developer documentation & setup guides for: make a website


## 📄 Target File(s)
### Target File: `README.md`
```
# Project
Production ready.

```

## 🔍 Relevant Code Symbols
--- File: engine/providers/base_provider.py (Lines 43-52) ---
    def generate(
        self,
        prompt: str,
        history: Optional[List[Dict[str, Any]]] = None,
        system_prompt: Optional[str] = None,
        on_chunk: Optional[Callable[[str], None]] = None,
        **kwargs
    ) -> ProviderResponse:
        """Executes completion or tool call, invoking on_chunk for partial tokens if supported."""
        pass

--- File: engine/providers/cerebras_provider.py (Lines 33-163) ---
    def generate(
        self,
        prompt: str,
        history: Optional[List[Dict[str, Any]]] = None,
        system_prompt: Optional[str] = None,
        on_chunk: Optional[Callable[[str], None]] = None,
        **kwargs
    ) -> ProviderResponse:
        api_key = self._get_api_key()
        if not api_key:
            return ProviderResponse(
                text="",
                model_name=self.model_name,
               
... [Remaining lines truncated for context budget]
```

## 🔍 Relevant Code Symbols
--- File: tests/test_automations_and_attachments.py (Lines 22-30) ---
    def setUp(self):
        self.test_dir = tempfile.mkdtemp()
        self.db_path = os.path.join(self.test_dir, "test_automations.db")
        self.db = DatabaseManager(self.db_path)
        
        # Create a sample test code file
        self.sample_code_file = os.path.join(self.test_dir, "test.py")
        with open(self.sample_code_file, "w", encoding="utf-8") as f:
            f.write("# Sample test code\nprint('Hello from test.py')\n")

--- File: tests/test_coding_agent_v6.py (Lines 64-107) ---
    def setUp(self):
        self.temp_dir = tempfile.TemporaryDirectory()
        self.workspace = Path(self.temp_dir.name).resolve()

        self.safety = SafetyController(self.workspace)
        self.context_mgr = ContextFileManager(self.workspace, self.safety)
        self.tools = ToolEcosystem(self.workspace, self.context_mgr, self.safety)
        self.memory = LongContextMemory(db_path=self.workspace / ".memory.db")
        self.model_router = CodingModelRouter()

        self.analyzer = ProjectAnalyzer(self.workspace)
        self.indexer = CodebaseIndexer(self.workspace)
        self.context_engine = ContextEngine(self.indexer, self.analyzer, self.memory)
        self.sandbox = SandboxRunner(self.workspace)
        self.tool_sys = ToolSystem(self.workspace, self.safety, self.sandbox, self.analyzer, self.indexer)
        self.checkpoint_mgr = CheckpointManager(self.tool_sys)
        self.task_mgr = PersistentTaskManager(db_path=self.workspace / ".tasks.db")

        # Mock online LLM calls for deterministic offline unit testing
        self._llm_patcher = patch.object(
            BaseAgent,
            "call_llm",
            return_value={
                "success": True,
                "text": (
                    "```python file:backend/server.py\n"

--- File: tests/test_collab_whiteboard.py (Lines 26-28) ---
    def setUp(self):
        self.canvas = WhiteboardCanvas()
        self.canvas.resize(800, 600)

--- File: tests/test_collab_whiteboard.py (Lines 249-251) ---
    def setUp(self):
        self.ide = CodingIdeView()
        self.ide.resize(1000, 700)

--- File: tests/test_database.py (Lines 13-16) ---
    def setUp(self):
        self.temp_dir = tempfile.TemporaryDirectory()
        self.db_path = Path(self.temp_dir.name) / "test_sage.db"
        self.db = DatabaseManager(self.db_path)

--- File: tests/test_ide_html_models_and_file_ops.py (Lines 28-31) ---
    def setUp(self):
        self.temp_dir = tempfile.mkdtemp(prefix="sage_ide_test_")
        self.workspace = Path(self.temp_dir)
        self.ide = CodingIdeView(workspace_path=self.workspace)

--- File: tests/test_image_credit_flow.py (Lines 17-21) ---
    def setUp(self):
        self.provider = ImageProvider()
        self.db = get_db()
        # Reset credits to 25 for predictable tests
        self.provider.refill_credits(25)

--- File: tests/test_knowledge_and_themes.py (Lines 17-19) ---
    def setUp(self):
        self.memory_manager = MemoryManager()
        self.vector_db = get_vector_db()

--- File: tests/test_multi_agent_orchestrator.py (Lines 62-84) ---
    def setUp(self):
        self.decomposer = TaskDecomposer()
        self.router = AgentRouter()
        self.workflow = WorkflowManager(agent_router=self.router)
        self.reflection = SelfReflection()
        self.memory = MemoryManager()
        self.orchestrator = AgentOrchestrator()

        self._llm_patcher = patch.object(
            BaseAgent,
            "call_llm",
            return_value={
                "success": True,
                "text": (
                    "```python\ndef celsius_to_fahrenheit(c: float) -> float:\n    return (c * 9.0 / 5.0) + 32.0\n```\n"
                    "Comprehensive solution and documentation."
                ),
                "model_name": "Sage Mock Core",
                "provider_id": "test_mock",
                "citations": []
            }
        )
        self._llm_patcher.start()

--- File: tests/test_new_features.py (Lines 15-18) ---
    def setUp(self):
        self.test_dir = tempfile.mkdtemp()
        self.db_path = os.path.join(self.test_dir, "test_usage.db")
        self.db = DatabaseManager(self.db_path)



## 🔗 Upstream Dependencies
**From `TASK-003` (backend):**
/* SAGE Backend Agent Offline Implementation */
// Generated for: Backend Task: Implement backend REST APIs & auth business logic for: Review acti

**From `TASK-004` (frontend):**
/* SAGE Frontend Agent Offline Implementation */
// Generated for: Frontend Task: Implement responsive UI, components & client integration for: Rev

**From `TASK-005` (qa):**
/* SAGE QA Agent Offline Implementation */
// Generated for: QA Task: Write automated test suites for backend APIs & UI for: Review active fi

**From `TASK-007` (devops):**
/* SAGE DevOps Agent Offline Implementation */
// Generated for: DevOps Task: DevOps containerization & CI/CD deployment configuration for: Revie

## 💡 Architectural Decisions & Conventions
- **Implementation: Review active file**: Engineered using 11-stage autonomous loop with python
- **Implementation: Build full stack analytics dashboar**: Engineered using 11-stage autonomous loop with unknown
- **Implementation: Build a production-grade course enr**: Engineered using 11-stage autonomous loop with unknown

## 🏗️ Project Architecture Overview
- Project Name: New folder (2)
- Architecture Type: Backend REST API / Microservice
- Primary Language: python
- Frameworks: Flask

## 📁 Project Architecture
- **Backend**: RESTful API server with SQLite persistence (`backend/`)
- **Frontend**: Modern responsive web application (`frontend/`)
- **DevOps**: Docker and compose containerization (`Dockerfile`, `docker-compose.yml`)
- **Tests**: Automated unit and integration test suites (`tests/`)

## 🛠️ Quick Start
### Local Setup
```bash
# 1. Install dependencies
pip install -r requirements.txt

# 2. Run backend server
python backend/server.py

# 3. Run automated tests
python -m unittest discover -s tests
```

### Docker Deployment
```bash
docker-compose up --build
```

## 📖 API Reference
| Method | Endpoint | Description |
|---|---|---|
| GET | `/api/health` | Service health status |
| GET | `/api/items` | List active items |
